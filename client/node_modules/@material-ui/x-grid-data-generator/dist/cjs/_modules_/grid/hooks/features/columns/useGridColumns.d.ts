import { GridApiRef } from '../../../models/api/gridApiRef';
import { GridColumns } from '../../../models/colDef/gridColDef';
export declare function useGridColumns(apiRef: GridApiRef, { columns }: {
    columns: GridColumns;
}): void;
