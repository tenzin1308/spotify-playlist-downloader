import { GridPaginationState } from './gridPaginationState';
import { GridState } from '../core/gridState';
import { GridRowId } from '../../../models/gridRows';
export declare const gridPaginationSelector: (state: any) => GridPaginationState;
export declare const gridPaginatedVisibleSortedGridRowIdsSelector: import("reselect").OutputSelector<GridState, GridRowId[], (res1: GridPaginationState, res2: GridRowId[]) => GridRowId[]>;
